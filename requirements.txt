fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1
python-multipart==0.0.20
pytest==8.3.4
httpx==0.28.1
pillow==11.1.0
imageio==2.36.1
imageio-ffmpeg==0.5.1
jsonschema==4.23.0
boto3>=1.34,<2
scikit-learn==1.6.1
# torch / timm / sentence-transformers intentionally omitted: relay-ml runs
# GRADING_MODE=bedrock_only + EMBEDDING_MODEL=bedrock-titan, so the local CNN
# grader and local sentence-transformers embedder are never used. Their imports
# are lazy (see app/models/cnn.py, app/pipelines/embed.py) and raise a clear
# error if those code paths are ever invoked without the heavy deps installed.
